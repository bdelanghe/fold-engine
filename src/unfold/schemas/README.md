## Schemas

Canonical JSON schemas used by tests and docs. Includes schema.org profiles for
WebSite, WebPage, WebPageElement, CreativeWork, and DefinedTerm, plus shared
datatype definitions in `datatypes.schema.json`.
