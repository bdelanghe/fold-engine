{
  "tasks": {
    "validate": "deno run -A scripts/validate_frontmatter.ts",
    "build": "deno task validate && deno run -A https://deno.land/x/lume/cli.ts",
    "dev": "deno run -A https://deno.land/x/lume/cli.ts -s"
  }
}
